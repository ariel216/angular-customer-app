import { LiteralMonthPipe } from './literal-month.pipe';

describe('LiteralMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new LiteralMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
